{
  "id": "hero",
  "name": "Hero",
  "type": "slice",
  "display": "default",
  "fields": {
    "title": {
      "config": {
        "type": "text",
        "name": "Title",
        "placeholder": "ABC Agency",
        "format": "headingOne"
      }
    },
    "description": {
      "config": {
        "type": "text",
        "name": "Description",
        "placeholder": "We are a design and innovation company. Find growth for your brand.",
        "format": "paragraph"
      }
    },
    "button": {
      "config": {
        "type": "text",
        "name": "Button",
        "placeholder": "Hello",
        "format": "paragraph"
      }
    }
  }
}
