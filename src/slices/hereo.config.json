{
  "id": "hero",
  "name": "Hero",
  "type": "slice",
  "display": "default",
  "fields": {
    "title": {
      "config": {
        "type": "text",
        "name": "Title",
        "placeholder": "ABC Agency",
        "format": "headingOne"
      }
    },
    "description": {
      "config": {
        "type": "text",
        "name": "Description",
        "placeholder": "We are a design and innovation company. Find growth for your brand.",
        "format": "paragraph"
      }
    },
    "buttons": {
      "config": {
        "type": "array",
        "name": "Button",
        "placeholder": {}
      },
      "fields": {
        "title": {
          "config": {
            "type": "text",
            "name": "Label",
            "placeholder": "Contact"
          }
        },
        "kind": {
          "config": {
            "type": "text",
            "name": "Kind",
            "placeholder": "Primary"
          }
        }
      }
    }
  }
}
