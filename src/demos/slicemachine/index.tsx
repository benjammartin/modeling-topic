import * as Layout from '@/components/layout/layout';

import { Fragment } from 'react/jsx-runtime';

const SliceMachine = () => {
  return (
    <Fragment>
      <Layout.Root>
        <Layout.Main>slICES</Layout.Main>
        <Layout.Sidebar>osef</Layout.Sidebar>
      </Layout.Root>
    </Fragment>
  );
};

export default SliceMachine;
